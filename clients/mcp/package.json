{
  "name": "@contextos/mcp",
  "version": "0.1.0",
  "description": "ContextOS MCP server — thin tools over Confirmed POST /context (Proposed agent wiring).",
  "type": "module",
  "bin": {
    "contextos-mcp": "./dist/bin.js"
  },
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p ./",
    "start": "tsx src/bin.ts",
    "test": "vitest run",
    "lint": "tsc -p ./ --noEmit"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3",
    "vitest": "^1.6.0"
  }
}
